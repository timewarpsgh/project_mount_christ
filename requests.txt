

















cmds
    # compile .proto file
    protoc --python_out=. .\login.proto


libs needed:
    front end
        pygame, pygamegui
    network
        protobuf
    back end
        asyncio, sqlalchemy

packet format
    2 bytes(opcode),
    2 bytes(len of content),
    x bytes(content for object)